<h1>Home page!</h1>

{{#each post}}
    <div class="card" >
        <h3>{{title}}</h3>
        <p>{{content}}</p>
        <p>Author: {{User.username}}</p>
        {{#each Comments}}
        <div class="card">
            <p>{{comment}}</p>
        </div>
        {{/each}}
    </div>
{{/each}}