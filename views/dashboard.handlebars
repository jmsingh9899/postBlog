

{{#each data}}
    <div class=" post card" id="myPost{{id}}">
        <h3>{{title}}</h3>
        <textarea id="{{id}}">{{content}}</textarea>
        <button class="editBttn">Edit</button>
        <button class="deleteBttn">Delete</button>
    </div>
{{/each}}
<script src='./js/dashboard.js'></script>